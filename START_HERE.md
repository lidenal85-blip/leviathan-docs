# START_HERE
### точка входа для любого агента/LLM/сессии — агентной или ручной через чат

> **Обязательное правило:** любая сессия, внёсшая изменение в состоянии системы/проекта, обновляет раздел «SNAPSHOT» ниже и ставит дату. Файл без актуальной даты внизу — это сигнал, что его нельзя доверять без проверки.

---

## 1. Где что лежит (проверено 2026-07-05)

| Сущность | Канонический путь | Признак |
|---|---|---|
| Документация (планы, роадмапы, аудиты) | `/opt/leviathan-docs-main/` | синхронизирован с `origin/main`, 0 разницы |
| База знаний (RAG, semantic search) | `/opt/leviathan-docs-main/knowledge/` + `/opt/leviathan-docs-main/scripts/index_knowledge.py` | создано 2026-07-05, работает на Gemini embeddings + SQLite |
| Секреты (Gemini/Groq пулы) | `secrets-vault` сервис, API `:8400`, проект `_shared` | единственный источник для Gemini/Groq. GitHub-токены — исключение, хранятся локально в `.git/config` каждого репо |
| Основной движок (agent core) | `/opt/leviathan_engine/agent_service/` | сервис `leviathan_agent`, .env в `agent_service/.env` |
| Старый/дублирующий клон доксов | `/opt/leviathan-docs/` (без `-main`) | отставал от origin на 1 коммит, использовался ошибочно в сессии 2026-07-05. **Не использовать**, требует решения об удалении/архиве |
| Старые отчёты (архив, не источник истины) | `/root/analysis/` (44 файла), `/root/docs/` (частично байт-в-байт дубликат analysis) | не актуализированы, не трогать как источник истины, только для истории |

## 2. Модели в активном использовании (проверено 2026-07-05)

- **Gemini по умолчанию в движке:** `gemini-3.1-flash-lite` (исторически был `gemini-2.0-flash-lite` — **устарел, больше не использовать**, источник — `agent_service/agent/core.py`)
- **Gemini embeddings:** `models/gemini-embedding-001` (старое имя `text-embedding-004` больше не существует в API)
- В `vocalplatform/core/ai/gemini_router.py` могут быть другие модели для своих задач — проверять `TASK_MODEL_MAP` на месте, не верить этому файлу со временем
- **Правило:** при каждом выборе версии модели в коде/документации — проверять актуальность через API (`genai.list_models()`), не верить названиям в старых файлах на слово

## 3. Карта проектов (`/opt/*`)

Полный список с описаниями — `docs/projects/INDEX.md` в этом репозитории. Ключевые для текущей работы:

- `/opt/leviathan_engine/` — ядро (agent_service/, core/, key_pool/)
- `/opt/arbitr_cockpit/` — Kwork-пайплайн (Triage → LISA-2 → Response Writer)
- `/opt/tszh-bot/` — SaaS для УК (планирование, не в активной разработке)
- `/opt/secrets_vault/` — хранилище секретов (см. раздел 1)
- `/opt/vocal_bot/`, `/opt/vocalplatform/`, `/opt/vocal_web/`, `/opt/voicestudio/` — voice-проекты, разные стадии перевода на vault (на 2026-07-05 — не завершен)

## 4. Статус безопасности (открытые вопросы, 2026-07-05)

- 28 из 28 старых Gemini-ключей (env + vault) были помечены Google как leaked — заменены на 8 новых работающих в `_shared/GEMINI_POOL_8KEYS`
- Найдены GitHub PAT в `.git/config` (открытым текстом) в 6 репозиториях, в т.ч. публичный `diet_platform` (`lidenal85-blip`) — **ПО решению владельца, не трогать**
- `leviathan_engine`, `vocal_bot`, `vocalplatform`, `vocal_web` ещё читают Gemini-ключи из своих `.env`, а не из vault напрямую — миграция на vault начата, не завершена

---

## SNAPSHOT (обновлять каждую сессию — пиши сверху, старые записи не удалять — это история)

### 2026-07-05 12:25 UTC (Claude, чат) — сверка + idea №3 (HTTP-шлюз для Buffy/внешних LLM без shell)
- Сверка: git на моей вершине (`ddf3084`), никто не пушил поверх. `kernel.py` StateMachine-баг всё ещё не исправлен, `leviathan_agent` всё ещё без рестарта с 07-02, оба токена (leviathan_engine + /root) всё ещё живы/не ротированы (соответствует решению владельца не трогать). P2 (LAP vs ADR-002) всё ещё не закрыт формально (`P2_LAP_vs_ADR002_DECISION.md` — только описание вариантов от 07-03, не решение).
- Найдена ещё одна развилка: `/root/CLAUDE.md` (авто-генерируется `/opt/claude-wake/context_loader.py`, читает `buffy.py`/Codebuff) и `/root/.claude/claude.md` (читает MCP workflow, правил вчера) — два разных файла, 541 строка разницы, не слиты. **Не решено**, требует решения владельца (какой файл каноничен?).
- Собран `leviathan-ctx-api` (systemd, `/opt/leviathan_ctx_api/main.py`, FastAPI, **127.0.0.1:8410 только**, не открыт вовне) — тонкая HTTP-обёртка над `lev-ctx`, без своей логики. `GET /health`, `GET /ctx/pull`, `GET /ctx/tools`, `POST /ctx/push` (с тем же секрет-сканом). Все 4 эндпоинта проверены живыми запросами, включая отказ на фейковый токен и реальный push (id=c59db343fff969ef). **Важно:** `/ctx/push` занимает ~70-90с (холодный старт embedding-модели внутри memory.py), не быстрый endpoint.
- Урок: не делать `pkill -f main.py` — слишком широкий паттерн на сервере, где `main.py` — имя файла в десятке проектов.
- **Не сделано:** внешний доступ к `leviathan-ctx-api` (только localhost); аутентификация на API отсутствует (нужна, если будет открыт вовне).

### 2026-07-05 (Claude, чат) — параллельная сессия, до чтения этого файла
- Найден хардкод Vault-токена в `scripts/index_knowledge.py` (не был закоммичен, но лежал в рабочей копии этого же публичного репо) — перенёс в `VAULT_TOKEN_SHARED_RO` (env, см. `/root/.bashrc`), токен в коде больше нет
- Там же исправлен путь-баг: `KNOWLEDGE_DIR`/`DB_PATH`/`LAST_REINDEX_MARKER` ссылались на `/opt/leviathan-docs` без `-main` — тот самый депрекатед клон, про который выше написано «не использовать»
- Собран `lev-ctx pull/push` (`/root/bin/lev-ctx`) поверх уже существующего `memory.py` — для Buffy/чат-ботов без MCP. `pull` теперь читает именно этот файл первым. См. `claude.md → ИНСТРУМЕНТЫ БЕЗ MCP`
- Починен недостижимый `elif cmd == "cross-search"` в `memory.py` (ветка с project-скопом шла после более общей, никогда не срабатывала)
- `AGENT_STATE_IMPLEMENTATION`/kernel.py `StateMachine(initial_state=)` TypeError и 4-я (теперь выяснилось — 5-я/6-я) утечка токена — см. `MASTER_ROADMAP.md §6`, запись от 2026-07-04
- **Не сделано:** полный реиндекс knowledge/ (2 тестовых документа в БД всё ещё со старым путём в metadata); башра-ключи (`GEMINI_API_KEY`/`GEMINI_KEYS`, 4 шт) не проверены на принадлежность к 28 leaked

### 2026-07-05 — старт (на момент начала сессии, до правок)
- Два не синхронизированных клона доков (`leviathan-docs` и `leviathan-docs-main`)
- Все Gemini-ключи в env и vault — leaked (обнаружено в ходе сессии)
- Двигок ссылался на `gemini-2.0-flash-lite`, реально в использовании — `gemini-3.1-flash-lite`
- База знаний (knowledge/) создана, но пуста (0 заиндексировано)
- Диск: 2.8 Гб свободно из 30 Гб (91%)

### 2026-07-05 — конец (после правок)
- 8 новых работающих Gemini-ключей в `_shared/GEMINI_POOL_8KEYS` (vault)
- `agent_service/agent/core.py`: дефолтная модель → `gemini-3.1-flash-lite`
- Индексатор знаний работает, модель эмбеддинга → `gemini-embedding-001`, ключи — из vault без локальных копий
- 2 тестовых документа заиндексированы, семантический поиск подтверждён работающим
- `leviathan-docs-main` назначен каноническим, `knowledge/` и `scripts/` перенесены туда из старого клона
- **Не сделано** (осталось на следующий раз):
  - `leviathan_engine/.env`, `vocal_bot/.env`, `vocalplatform/.env`, `vocal_web/.env` — всё ещё свои копии ключей, не читают из vault напрямую
  - Старый клон `/opt/leviathan-docs` (без -main) не удалён, требует решения
  - `/root/analysis` + `/root/docs` дубликаты не разобраны, не вошли в knowledge/
  - Groq-пул (`GROQ_K1-5` в leviathan_engine) не проверялся на валидность

**RAM:** 1.1 Ги free из 3.8 Ги | **Load avg:** 3.18 | **Диск:** 2.8 Гб свободно из 30 Гб (91%) — зафиксировано 2026-07-05, проверять `df -h /` заново если с того времени прошло больше нескольких дней.