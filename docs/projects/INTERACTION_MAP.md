# 🔄 LEVIATHAN Interaction Map

How the 58 projects communicate and depend on each other.

## Data Flow Overview

```
                     ┌─────────────┐
                     │  Claude API  │
                     │   (External) │
                     └──────┬──────┘
                            │
┌─────────────┐     ┌──────▼──────┐     ┌──────────────┐
│   Gemini    │◄────┤ den4ik-claude├────►│  PostgreSQL  │
│   API      │     │ (MCP Client) │     │   (5434)     │
└─────────────┘     └──────┬──────┘     └──────────────┘
                           │
                  ┌────────▼────────┐
                  │ leviathan_engine │
                  │  ┌────────────┐  │
                  │  │ Agent Srv  │──┼──► MCP Tools (27)
                  │  │ MCP Server │──┼──► lev_bash, git, fs
                  │  │ Core Lib   │  │
                  │  └────────────┘  │
                  └────────┬────────┘
         ┌─────────────┬───┼───┬─────────────┐
         │             │   │   │             │
  ┌──────▼─────┐ ┌─────▼───▼───▼─────┐ ┌─────▼──────┐
  │  savdabot  │ │  Business Apps    │ │  Web Apps   │
  │  kwork_bot │ │  arbitr_cockpit   │ │  kinovibe   │
  │  tg_channel│ │  diet_platform    │ │  voicestudio│
  │  pelleto   │ │  fashion-stylist  │ │  outreach   │
  └────────────┘ └──────────────────┘ └────────────┘
```

## Core Dependencies

### Tier 1: Critical Path
```
den4ik-claude ──────► PostgreSQL (auth, sessions)
leviathan_engine ───► Redis (caching, queues)
leviathan_engine ───► Secrets Vault (credentials)
arbitr_cockpit ─────► PostgreSQL (business data)
savdabot ───────────► leviathan_engine (AI tasks)
```

### Tier 2: Service Dependencies
```
kinovibe ───────────► Nginx (reverse proxy)
voicestudio ────────► Nginx (reverse proxy)
pelleto-ai ─────────► leviathan MCP (HTTP:8300)
hermes_agent ───────► MCP Servers (native-mcp client)
```

### Tier 3: Infrastructure
```
All web projects ───► Nginx (ports 80/443)
All Python projects ─► Python 3.12 + venv
All services ───────► Systemd
```

## Communication Protocols

| Protocol | Usage | Projects |
|----------|-------|----------|
| **HTTP/REST** | API calls | arbitr, savda, kwork |
| **MCP/SSE** | AI tools | leviathan_mcp, pelleto |
| **WebSocket** | Real-time | vocalplatform |
| **gRPC** | Internal | (not yet used) |
| **Unix Socket** | Local IPC | PostgreSQL, Redis |

## External Integrations

| Service | Direction | Projects |
|---------|-----------|----------|
| Claude API (anthropic) | Outbound | den4ik-claude |
| Gemini API (google) | Outbound | leviathan_engine |
| OpenAI API | Outbound | leviathan_engine |
| Telegram Bot API | Outbound | savdabot, tg_channel |
| Kwork API | Outbound | kwork_bot |
| Cloudflare DNS | Outbound | pelleto-ai |
| GitHub API | Outbound | lev-cli, cursor config |

---
*Generated by LEVIATHAN Architect — 2026-07-03*
